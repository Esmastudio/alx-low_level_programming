#include "main.h"
/**
 * add - prints the addition of two integers
 * @a: first input
 * @b: second input
 * Return: value of addition
 */
int add(int a, int b)
{
	int addi = 0;

	addi = a + b;
	return (addi);
}
